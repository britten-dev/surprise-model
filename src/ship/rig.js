// The masts, spars, standing and running rigging, and the sails. Not built yet.
import * as THREE from 'three';

export function buildRig(cfg, mats, model, ctx) {
  const group = new THREE.Group();
  group.name = 'rig';
  return group;
}

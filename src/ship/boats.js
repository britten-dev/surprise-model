// The boats. Not built yet — see docs/MODULE-CONTRACT.md.
import * as THREE from 'three';

export function buildBoats(cfg, mats, model, ctx) {
  const group = new THREE.Group();
  group.name = 'boats';
  return group;
}

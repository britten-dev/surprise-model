// Dimensions for the head of the ship. See src/spec/parts/index.js for the shape
// of an entry and for the rule that every key must also appear in SPECS.md.
import { ft, m } from '../units.js';

export const HEAD_SPEC = {
};
